# Required: keys issued by Storyblocks (https://developer.storyblocks.com/register for test keys)
STORYBLOCKS_PUBLIC_KEY=your_public_key
STORYBLOCKS_PRIVATE_KEY=your_private_key

# Optional: defaults used when a tool call omits user_id / project_id. Leave blank for a personal install:
# project_id defaults to "storyblocks-mcp" and user_id to a random per-install id saved in ~/.storyblocks-mcp/user-id.
# Set them only when embedding in a multi-user app with its own ids. Letters, numbers, dashes, underscores only — never a name or email.
STORYBLOCKS_DEFAULT_USER_ID=
STORYBLOCKS_DEFAULT_PROJECT_ID=

# Optional overrides
# Directory for the generated user-id file
# STORYBLOCKS_STATE_DIR=~/.storyblocks-mcp
# STORYBLOCKS_BASE_URL=https://api.storyblocks.com
# Seconds until a signed request's EXPIRES timestamp lapses (max allowed by Storyblocks: 36h = 129600)
# STORYBLOCKS_EXPIRES_SECONDS=300
# HTTP timeout in milliseconds
# STORYBLOCKS_TIMEOUT_MS=30000
